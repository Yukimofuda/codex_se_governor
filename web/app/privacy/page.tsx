import Link from "next/link";

export default function PrivacyPage() {
  return <main className="legal-page"><div className="legal-shell"><Link href="/">← Codex SE Governor</Link><article className="glass-panel"><div className="eyebrow">PRIVACY NOTICE</div><h1>Local-first by default</h1><p>Projects, task artifacts, uploaded ZIP files, and validation results are processed in your browser. The MVP does not create an account, upload repository content, or store project data on the server.</p><h2>Local storage</h2><p>Projects and artifact revisions are stored in IndexedDB on the current device. Use Settings to delete them.</p><h2>Uploaded files</h2><p>ZIP bytes remain in browser memory for static inspection and are discarded when the page is closed or reloaded. Uploaded code is never executed.</p><h2>Logs and training</h2><p>The application does not send repository content to application logs or AI training services. Hosting infrastructure may retain standard request metadata that does not include locally processed files.</p><h2>Exports</h2><p>Generated downloads contain only the artifacts selected in the current local workspace. Review files before sharing them.</p><h2>Contact</h2><p>No public support identity has been configured for this MVP.</p></article></div></main>;
}
